# Loads the devShell from flake.nix, so cargo, rustfmt, clippy, just and prek
# all come from the toolchain pinned in flake.lock. Requires nix and direnv;
# `nix develop` does the same thing without direnv.
use flake
